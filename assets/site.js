(async()=>{const el=document.querySelector('[data-election-status]');if(!el)return;try{const r=await fetch('/data/election-status.json',{cache:'no-store'});if(!r.ok)return;const d=await r.json();const map={possible:'Possible early election — not yet called',called:'Provincial election called',scheduled:'Next scheduled provincial election'};el.querySelector('[data-status-label]').textContent=map[d.status]||d.status;el.querySelector('[data-status-detail]').textContent=d.detail||'';el.querySelector('[data-status-checked]').textContent='Last checked '+d.last_checked;}catch(e){}})();
